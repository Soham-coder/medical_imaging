#include <stdio.h>

int main(void)
{
    int i,j,value,r,g,b,factor;
    fp=fopen("colorfile.txt","rb");
    if(!fp)
    {
        printf("\n File not found!!!");
    }
    for(i=0;i<=255;i++)
    {

    }
    return 0;
}
